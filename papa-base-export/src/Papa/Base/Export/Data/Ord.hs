{-# LANGUAGE NoImplicitPrelude #-}

module Papa.Base.Export.Data.Ord(
    Ord(compare, (<), (<=), (>), (>=), max, min)
  , Down(Down)
  , comparing
) where

import Data.Ord (
    Ord(compare, (<), (<=), (>), (>=), max, min)
  , Down(Down)
  , comparing
  )
